/*--------------------------------*\
    $MIXINS
\*--------------------------------*\

/*
* Ancho
*/

mixin-max-width(width = page-max-width)
  max-width width
  margin-left auto
  margin-right auto

mixin-padding-small(a)
  a - padding
  /*Aplicando el mixin con valor custom
.section
  mixin-max-width(90%)*/